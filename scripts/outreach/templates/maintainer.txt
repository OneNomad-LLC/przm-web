Subject: {{framework}}'s convergence number (heads-up before we publish)

Hi {{firstName}},

I'm Matt Stvartak. I run OneNomad LLC and built przm.sh, an
open-source benchmark suite for AI reliability failure modes that
don't have a vendor-neutral standard yet.

{{specific_open}}

The v0.1 leaderboard shipped today with six adapter configurations
on it: baseline-Anthropic Haiku 4.5 (sync + sequential reveal),
baseline-Azure gpt-5-mini, gpt-4o-mini (sync + sequential reveal),
and AutoGen RoundRobin on gpt-4o-mini. {{scores_line}}

The headline finding, held constant on gpt-4o-mini: on the sealed
6-fixture holdout, AutoGen collapsed 0 of 6 scenarios; the hand-rolled
baseline collapsed 5 of 6. On the 30-fixture combined run: AutoGen
10%, sync baseline 73%, sequential-reveal baseline 87%. The gap holds
after controlling for reveal protocol, so the framework is doing real
work beyond just letting agents see each other within a round.

Two asks before {{framework}}'s adapter lands on the leaderboard:

1. Adapter review. {{framework_adapter_status}}. The adapters live at
   https://github.com/OneNomad-LLC/przm-bench/tree/main/src/adapters/multiagent
   and PRs are welcome to fix anything that looks unfair.

2. Charter customer slot. First 3-5 framework vendors get a free signed
   receipt plus leaderboard placement on launch day, in exchange for a
   1-2 sentence quote we can cite. After that, $999/release. Charter
   form: https://przm.sh/vendor-cert

No reply needed if you'd rather not engage. We're publishing either
way; this is a courtesy heads-up so you're not learning it from HN.

Thanks,
Matt
matt@przm.sh
przm.sh
