.next
node_modules
.env*.local
tsconfig.tsbuildinfo
.vercel

# Outreach recipient DB + send log. Contains per-recipient pitch
# notes about real people and companies — not for public repos.
local-data/

# Dev-only email preview output if ever generated
.email-previews/

# X posting log — append-only JSONL with real tweet IDs from sends.
scripts/social/*.log
